package com.example.mutationtest.scenario3.complex;

/**
 * Categoria de precificação de um {@link Product}.
 * Ver {@link PricingService} para as regras aplicadas a cada uma.
 */
public enum PricingRule {
    STANDARD,
    PREMIUM,
    SEASONAL
}
